version: '3.8'
services:
  web:
    image: holeminh/midterm-app:latest
    restart: unless-stopped
    depends_on:
      - mongo
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/midtermdb
      - NODE_ENV=production
    volumes:
      - uploads:/app/uploads
  mongo:
    image: mongo:6.0
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
volumes:
  uploads:
  mongo-data:
